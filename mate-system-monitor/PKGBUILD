#Mantainer: Giovanni Ricciardi <kar98k.sniper@gmail.com>

pkgname=mate-system-monitor
pkgver=1.2.1
pkgrel=1
pkgdesc="A system monitor for MATE"
arch=(i686 x86_64)
license=('GPL')
depends=('libwnck' 'libgtop' 'gtkmm' 'librsvg' 'mate-icon-theme')
makedepends=('mate-doc-utils' 'pkgconfig' 'intltool')
groups=('mate-extras')
options=(!emptydirs !libtool)
url="http://mate-desktop.org"
install=mate-system-monitor.install
source=(http://pub.mate-desktop.org/releases/1.2/$pkgname-$pkgver.tar.xz ./mate-system-monitor-1.2.1.patch)
sha256sums=('31714d960eac8e5559178b60e76fe32ded6afd8d716d578aa39f3271f01f4578' '2192728c8f1a8a16920e0a07efb1084da799062398a7deb7980892fd8770688f')

build() {
	cd "${srcdir}/${pkgname}-$pkgver"
	./autogen.sh --prefix=/usr --sysconfdir=/etc \
		--libexecdir=/usr/lib/${pkgname} \
		--localstatedir=/var --disable-static \
		--disable-scrollkeeper \
		--disable-schemas-install

	# there is some pre-compiled files on the tar
	make clean
	patch -p1 -i $srcdir/$pkgname-$pkgver.patch
	make || return 1
}

package() {
	cd "${srcdir}/${pkgname}-$pkgver"

	make MATECONF_DISABLE_MAKEFILE_SCHEMA_INSTALL=1 DESTDIR="${pkgdir}" install || return 1
	#rm -f ${pkgdir}/usr/lib/*.la # can be used options=(!libtools)

	install -m755 -d "${pkgdir}/usr/share/mateconf/schemas"
	mateconf-merge-schema "${pkgdir}/usr/share/mateconf/schemas/${pkgname}.schemas" --domain ${pkgname} ${pkgdir}/etc/mateconf/schemas/*.schemas || return 1
	rm -f ${pkgdir}/etc/mateconf/schemas/*.schemas
}
