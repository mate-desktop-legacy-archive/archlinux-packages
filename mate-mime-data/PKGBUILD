pkgname=mate-mime-data
pkgver=1.1.0
pkgrel=1
pkgdesc="The base MIME and Application database for MATE"
arch=('any')
license=('GPL')
depends=('shared-mime-info')
makedepends=('intltool')
url="http://matsusoft.com.ar/projects/mate/"
groups=('mate')
source=(./${pkgname}-${pkgver}.tar.bz2)
sha256sums=('fd180ed790ff5934be75b4acbe6cd9889d07bf63411289d0c5281f574bfbb8d2')


build() {
    cd "${srcdir}/$pkgname-$pkgver"
    ./autogen.sh --prefix=/usr --sysconfdir=/etc --localstatedir=/var || return 1
    make || return 1
}

package() {
    cd "${srcdir}/$pkgname-$pkgver"
    make DESTDIR="${pkgdir}" install

    install -m755 -d "${pkgdir}/usr/lib"
    mv "${pkgdir}/usr/share/pkgconfig" "${pkgdir}/usr/lib/"
}
