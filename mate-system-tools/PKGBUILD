#Mantainer: Giovanni "Talorno" Ricciardi <kar98k.sniper@gmail.com>

pkgname=mate-system-tools
pkgver=1.6.0
pkgrel=1
pkgdesc="An extension for opening terminals in arbitrary local paths"
arch=('i686' 'x86_64')
url="http://manny.cluecoder.org/packages/nautilus-open-terminal"
license=('GPL')
depends=('mate-file-manager' 'liboobs' 'system-tools-backends')
makedepends=('pkgconfig' 'intltool')
install=mate-system-tools.install
options=('!libtool')
group=('mate-extras')
source=(http://pub.mate-desktop.org/releases/1.6/$pkgname-$pkgver.tar.xz)
sha256sums=('3e545ae4462332cd4e4b1c0b2f0e41a7fbacbb86be3e95254155e92ed249a14a')

build() {
	cd "$srcdir/$pkgname-$pkgver"
	sed -i s/AM_CONFIG_HEADER/AC_CONFIG_HEADERS/ configure.in #FIXME: >1.6.0
	sed -i s/AM_PROG_CC_STDC/AC_PROG_CC/ configure.in #FIXME: >1.6.0
	./autogen.sh --prefix=/usr 

	#fix non-standard directory
	sed -i 's|${prefix}/etc|/etc|' Makefile

	make || return 1
}

package() {
    cd "${srcdir}/$pkgname-$pkgver"

    make DESTDIR="$pkgdir/" install
}
