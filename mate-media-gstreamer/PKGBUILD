#Mantainer: Giovanni "Talorno" Ricciardi <kar98k.sniper@gmail.com>
#Contributor: Xpander <xpander0@gmail.com>

pkgname=mate-media-gstreamer
pkgver=1.6.0
pkgrel=1
pkgdesc="MATE Media Tools"
arch=(i686 x86_64)
license=('GPL')
depends=('gstreamer0.10-base-plugins' 'mate-panel' 'libnotify')
makedepends=('mate-doc-utils' 'pkgconfig' 'mate-settings-daemon' 'intltool' 'mate-icon-theme')
conflicts=('mate-media')
options=(!emptydirs !libtool)
url="http://mate-desktop.org"
install=mate-media.install
source=(http://pub.mate-desktop.org/releases/1.6/mate-media-$pkgver.tar.xz)
sha256sums=('baf0ac85685a376107feee47b372b678a86404ce215b19c7fc259b012a3694a6')

build() {
	cd "${srcdir}/mate-media-$pkgver"
	./autogen.sh --prefix=/usr --sysconfdir=/etc \
		--libexecdir=/usr/lib/mate-media \
		--localstatedir=/var --disable-static \
		--disable-schemas-install \
		--disable-scrollkeeper \
		--disable-pulseaudio \
		--enable-profiles --enable-grecord \
		--enable-gstprops --enable-gstmix
	# brute force
	mate-doc-prepare --force --copy
	automake
	autoconf
	autoreconf -i --force
	aclocal
	intltoolize --automake --copy --force
	automake --add-missing

	# Optional Features:
	#    --enable-pulseaudio     Enable PulseAudio support [default=auto]
	#    --enable-profiles       enable mate media profiles
	#    --enable-grecord        enable mate sound recorder
	#    --enable-gstprops       enable gstreamer properties
	#    --enable-gstmix         enable gstreamer mixer

	make || return 1
}

package() {
    cd "${srcdir}/mate-media-$pkgver"

    make DESTDIR="$pkgdir/" install
}

