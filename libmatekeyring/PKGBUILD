# Contributor: Giovanni "Talorno" Ricciardi <kar98k.sniper@gmail.com>
# Contributor: Xpander <xpander0@gmail.com>
# Contributor: Martin Wimpress <code@flexion.org>

pkgname=libmatekeyring
pkgver=1.6.0
pkgrel=2
pkgdesc="MATE keyring client library"
url="http://mate-desktop.org"
arch=('i686' 'x86_64' 'armv6h' 'armv7h')
license=('GPL' 'LGPL')
depends=('dbus' 'glib2' 'libgcrypt')
makedepends=('docbook-xml' 'mate-common' 'perl-xml-parser')
options=('!emptydirs' '!libtool')
groups=('mate')
source=(http://pub.mate-desktop.org/releases/1.6/${pkgname}-${pkgver}.tar.xz)
sha256sums=('7ce673bbf7e2d20811b5197c9a79e813226c4ef8d2719f606ad6d98e5958cc99')

build() {
    cd ${srcdir}/${pkgname}-${pkgver}
    ./autogen.sh \
        --prefix=/usr \
        --libexecdir=/usr/lib/mate-keyring
        --disable-static \
    make
}

package() {
    cd ${srcdir}/$pkgname-$pkgver
    make DESTDIR="${pkgdir}" install
}
