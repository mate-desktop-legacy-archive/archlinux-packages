#Mantainer: Giovanni Ricciardi <kar98k.sniper@gmail.com>

pkgname=mate-screensaver
pkgver=1.6.1
pkgrel=1
pkgdesc='Screensaver for MATE'
arch=('i686' 'x86_64' 'armv6h' 'armv7h')
url="http://mate-desktop.org"
license=('GPL')
depends=('gtk2' 'dbus' 'mate-desktop')
makedepends=('pkgconfig' 'intltool' 'mate-doc-utils' 'glib2')
options=('!libtool' '!emptydirs')
source=(http://pub.mate-desktop.org/releases/1.6/$pkgname-$pkgver.tar.xz
		mate-screensaver.pam)
install=mate-screensaver.install
sha256sums=('b04801e68d013c899cdfefceed25e28040c21197a2da40cd650be52ac643843c'
            '2744d1fc39da46fc681b7cf1c9230d2035b2bb8d9f510213fbe439c114eb76a2')

build() {
	# this is a hack for tarballs on github
	cd $srcdir/${pkgname}-$pkgver

	./configure \
		--prefix=/usr --sysconfdir=/etc \
		--mandir=/usr/share \
		--disable-static \
		--with-xscreensaverdir=/usr/share/xscreensaver/config \
		--with-xscreensaverhackdir=/usr/lib/xscreensaver \
		--with-mit-ext \
		--libexecdir=/usr/lib/${pkgname} || return 1

	make || return 1
}

package() {
    cd "${srcdir}/$pkgname-$pkgver"

    make DESTDIR="$pkgdir/" install
}
